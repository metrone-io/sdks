{
  "name": "@metrone/agent-edge",
  "version": "1.0.0",
  "description": "Metrone AI Agent Edge Tracker — deployable Cloudflare Worker template that captures AI agent visits (GPTBot, ChatGPT-User, PerplexityBot, ...) on a customer's own zone and forwards them to Metrone",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.2",
    "vitest": "^2.0.0",
    "wrangler": "^4.67.0"
  }
}
